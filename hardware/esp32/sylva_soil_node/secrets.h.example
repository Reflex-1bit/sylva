#pragma once

// Copy this file to secrets.h and fill in real values.
// secrets.h is gitignored.

#define WIFI_SSID     "your-farm-wifi"
#define WIFI_PASS     "your-password"

// Sylva ingest endpoint (Phase 1 stub — point at your API when ready)
#define INGEST_URL    "https://your-api.example.com/api/v1/sensors/ingest"
#define DEVICE_ID     "sylva-esp-001"
#define DEVICE_TOKEN  "change-me"

// Optional fixed install location (0,0 = omit from payload)
#define DEVICE_LAT    0.0
#define DEVICE_LON    0.0

// Seconds between samples (default 30 min)
#define SAMPLE_INTERVAL_S 1800
